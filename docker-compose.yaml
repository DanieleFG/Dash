version: '3.8'

services:
  web:
    image: php:8.2-cli
    container_name: dashboard
    volumes:
      - .:/usr/src/dash
    working_dir: /usr/src/dash
    command: php -S 0.0.0.0:8081
    # command: php ./index.php
    ports:
      - "8081:8081"
